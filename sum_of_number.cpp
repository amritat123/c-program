#include<iostream>
using namespace std;
int main(){
    int number,sum=0,count=0;
    cout<<"enter your number";
    cin >> number;

    while (number>=count)
         if (count%2==0)
        sum=sum+count;
        count++;
    cout<< sum;
    return 0;

}